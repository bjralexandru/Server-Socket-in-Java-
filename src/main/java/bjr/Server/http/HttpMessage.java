package bjr.Server.http;

public abstract class HttpMessage {
    // This class is abstract because both the HttpRequest and HttpResponse
    // are a type of HttpMessage. This will contain everything the other classes would need

}
